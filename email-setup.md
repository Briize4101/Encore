# Email 通知設定

網站右上角「Email 通知」可分別開啟新活動、明天售票／登記開始、明天抽選／登記截止。寄往 Supabase 登入帳號已驗證的信箱，不需要另外提供收件人。Google 登入也適用。

## 寄信服務
目前實作 Resend。建立 Resend 帳號、驗證你的寄件網域，並建立具有寄信權限的 API key。官方說明：https://resend.com/docs/dashboard/domains/introduction

在此網站 `.env` 加入以下兩行，保留既有 Supabase 設定：

```
RESEND_API_KEY=填入你的 Resend API key
EMAIL_FROM=Encore <notify@你的已驗證網域>
```

重新啟動 `start.cmd`，登入網站並開啟想接收的 Email 通知。API key 僅存於伺服器，無需傳到聊天或放進前端。

Resend 測試寄件地址有收件人限制，正式寄給其他會員須使用已驗證的寄件網域。Supabase 用來寄註冊驗證信的設定，不等於活動通知寄信服務。

## 運作方式與限制
- 每 5 分鐘檢查已收錄資料；新活動在偵測到後寄出。來源原有更新頻率不變，並非官方發布的即時推播。
- 首次開啟與新增追蹤團體時，以現有資料為起點，不補寄全部歷史活動。
- 「明天」依 Asia/Taipei 計算，每天台灣時間 09:00 起寄提醒。若 09:00 未啟動、但當天稍晚啟動，仍可寄當天應提醒的通知。錯過整天不補寄標為「明天」的過期信。
- 只使用已解析且含明確時區的售票時間、Weverse/Lawson 登記與抽選期間；不會拿演唱會日期當售票日期，無法解析者不寄定時提醒。
- 信件包含官方連結、台灣／日本時間及已知的 Weverse 前置登記要求。
- 通知設定、追蹤名單副本與寄送紀錄存於本機 `data/email-notifications.json`，不公開給前端、不需新增 SQL。請保留此檔以避免重複寄信；帳號通知設定不會自動同步到其他伺服器。
- 在本站儲存追蹤團體時會更新通知名單。直接在 Supabase 修改追蹤表不會自動更新此本機副本。
- 網站服務及電腦必須保持執行並可連網；關閉瀏覽器不影響，關閉電腦則無法寄信。要全天候運作需另外部署常駐服務。
- 每個通知使用固定寄送識別碼，失敗 15 分鐘後重試。若超過 23 小時仍無法確認結果，停止重試並在通知設定顯示錯誤，避免超過供應商去重期限重寄。
- 可取消所有勾選以停止通知。寄信 API 接受不保證送達收件匣，實際投遞狀態請查 Resend。
